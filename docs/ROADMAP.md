# 演进边界

v0.2重点是把实际框架执行、网页交互、人工审批、检查点、启动与测试串联，不追求将所有底层适配默认塞入一个Agent。

下一步按真实需求选择：多租户身份与授权、持久任务调度/集群租约、外部副作用核对、真实模型评测集、向量检索接入与embedding成本、审计与数据删除。每项先定义失败案例与验收，不以安装更多框架代替可靠性。

A2A、任意时间旅行、多Agent自由对话、自动学习和训练集群不属于本次完成范围。
