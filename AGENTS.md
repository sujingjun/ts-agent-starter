# 项目协作规则

先读 README、MIGRATION-MATRIX 和 VALIDATION，确认“源码存在”和“已验证”区别。修改前查对应源文件与测试，只改任务涉及的模块。新工具必须有参数/输出/权限/超时测试。安全边界由代码执行，不由模型或 Skill 自授权限。

核心修改运行 npm test 与 npm run eval；服务修改还要实际构建对应子应用。未运行的命令不得写成已通过。保留原许可与来源。不能因为新增目录或文档就把迁移状态改为100%。

常规低风险代码与文档修改无需反复请求用户确认；改变外部权限、上线、发消息、支付或删除数据需要明确授权。遇到外部依赖无法使用时完成可完成部分并准确报告，不用假实现冒充协议或服务。
